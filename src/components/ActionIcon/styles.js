import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  iconContainer: {
    alignItems: 'center',
  },
  statsLabel: {
    color: '#fff',
    fontSize: 15,
    fontWeight: '600',
    marginTop: 5,
  },
});

export default styles;
